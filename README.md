# roiutils

roiutils is a Python library for creating, manipulating, and visualizing ROIs for neuroimaging workflows.

Current MVP focus:
- Load indexed atlas files
- Select a subset of ROI IDs or labels
- Build ROI masks for downstream visualization on MNI templates

## Installation

```bash
pip install -e .[dev]
```

## Status

This repository is in early development.
